package pagination
object Pagination {
//  implicit class TakeUntilIteratorWrapper[T](iterator: Stream[T]) {
//    def takeUntil(predicate: T => Boolean): Stream[T] = {
//      iterator.span(!predicate(_)) match {
//        case (head, tail) => head append tail.take(1)
//      }
//    }
//  }
}
